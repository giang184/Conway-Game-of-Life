// import { Cell, GameBoard } from '../src/lib/GameBoard';

// describe('class GameBoard', () => {
//   describe('method getNeighbors()', () => {
//     it('should correctly return the correct neighbors', () => {
//       const game = new GameBoard(3, 3);

//       game.board[0][0].state = true;
//       game.board[1][1].state = true;
//       game.board[2][2].state = true;

//       array = game.getNeighbors(1, 1);

//       expect(array.length).toEqual(8);
//     });
//   });
// });
